
<nav aria-label="Page navigation example">

  <ul class="pagination">
    {% if current == 1 %}
      <li class="page-item disabled">
    {% else %} 
      <li class="page-item">
    {% endif %}
      <a class="page-link" href="{{ url('index', {'page': current-1 }) }}" aria-label="Previous">
        <span aria-hidden="true">&laquo;</span>
      </a>
    </li>
    {% for i in 1..7 %}
    {% if (i) == current %}
      <li class="page-item active"><a class="page-link" href="{{ url('index', {'page': i }) }}">{{ i }}</a></li>
    {% else %}
      <li class="page-item"><a class="page-link" href="{{ url('index', {'page': i }) }}">{{ i }}</a></li>
    {% endif %}
    {% endfor %}
    {% if current == 7 %}
      <li class="page-item disabled">
    {% else %} 
      <li class="page-item">
    {% endif %}
     <a class="page-link" href="{{ url('index', {'page': current }) }}" aria-label="Next">
        <span aria-hidden="true">&raquo;</span>
      </a>
    </li>
  </ul>
</nav>